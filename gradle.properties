# Project-wide Gradle settings.
org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8 -XX:+UseG1GC

# When configured, Gradle will run in incubating parallel mode.
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configureondemand=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK.
android.useAndroidX=true

# Kotlin code style for this project: "official" or "obsolete"
kotlin.code.style=official

# Enables namespacing of each library's R class so that its R class includes only
# the resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library.
android.nonTransitiveRClass=true

# Snapdragon 8 Elite / Samsung S25 Ultra build notes
# Hexagon NPU: Hexagon v79 DSP (HTP = Hexagon Tensor Processor)
# Adreno GPU: Adreno 830
# SoC: SM8750-AB
kenju.target.soc=sm8750
kenju.target.hexagon=v79
kenju.target.adreno=830
